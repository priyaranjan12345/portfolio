import 'package:flutter/material.dart';

class GithubProjectCard extends StatelessWidget {
  const GithubProjectCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
