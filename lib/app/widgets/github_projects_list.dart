import 'package:flutter/material.dart';

class GithubProjectsList extends StatelessWidget {
  const GithubProjectsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
